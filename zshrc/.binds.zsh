# ================================================================
# # 🔗 BINDINGS FOR CUSTOM FUNCTIONS
# ================================================================

# * Bind the function to a key sequence for quick access
bindkey '^[y' run_ystatus               # Alt+Y
bindkey '^[h' run_hypr_clients           # Alt+H

# Defined shortcut keys: [ALT] [R]
bindkey -M emacs '^[e' fuck-command-line
bindkey -M vicmd '^[e' fuck-command-line
bindkey -M viins '^[e' fuck-command-line